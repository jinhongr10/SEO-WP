# Server port
APP_PORT=3000

# WordPress REST
WP_BASE_URL=https://example.com
WP_USER=your_wp_user
WP_APP_PASSWORD=your_app_password
# WP_JWT=your_jwt_token

# Optional Cloudflare REST bypass header
# WP_REST_BYPASS_HEADER_NAME=X-LensCraft-REST-Token
# WP_REST_BYPASS_HEADER_VALUE=replace_with_a_long_random_secret

# WooCommerce REST API keys
WC_CONSUMER_KEY=ck_your_consumer_key
WC_CONSUMER_SECRET=cs_your_consumer_secret

# SFTP
SFTP_HOST=example.sftp.host
SFTP_PORT=22
SFTP_USER=master
SFTP_PASSWORD=your_sftp_password
# SFTP_PRIVATE_KEY_PATH=/absolute/path/to/key.pem

REMOTE_WP_ROOT=/home/master/applications/<app_id>/public_html
UPLOADS_RELATIVE=wp-content/uploads

# Runtime limits
CONCURRENCY=5
AI_BATCH_CONCURRENCY=10
PER_PAGE=100
MAX_PAGES=9999
DRY_RUN=true

# AI configuration. Vertex AI is recommended for server deployment.
LLM_PROVIDER=gemini
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=global
GOOGLE_APPLICATION_CREDENTIALS=/app/keys/vertex-sa.json
VERTEX_SA_JSON=./keys/vertex-sa.json
# GEMINI_API_KEY=your_gemini_api_key
# GENAI_FLASH_MODEL=gemini-2.5-flash

# Optional outbound proxy from inside the container
# CONTAINER_HTTPS_PROXY=http://proxy-host:proxy-port
# CONTAINER_HTTP_PROXY=http://proxy-host:proxy-port

RATE_LIMIT_MS=120
RETRY_COUNT=3
